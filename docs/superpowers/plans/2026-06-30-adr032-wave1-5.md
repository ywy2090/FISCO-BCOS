# ADR-032 Wave 1–5 Implementation Plan

**Branch:** `feat/adr-030-geth-naming`  
**Baseline:** `068d8ca7b` (P0–P6 complete)  
**Authority:** `bcos-evm/docs/adr/032-tier-e-symbol-retirement.md`

## Global Constraints

- Execute waves **strictly in order** (1 → 5); one commit (or small commit series) per wave PR.
- Before removing any symbol: `rg` monorepo for symbol returns zero non-doc, non-ADR production hits.
- Do **not** remove `*Execute` exported symbols before Wave 3 promotion.
- Wave 4 removes `*Execute` — audit entire monorepo including `transaction-executor`, aggregate headers.
- Run smoke tests after each wave: `ctest -R 'EthOrchestrationProfile|TxExecution|TxPipeline|Precompile|OpStackPrecheck|DeductIntrinsic|ExecuteViaHost'`.
- Update ADR-032 appendix timeline row when wave merges.
- Do not commit `.superpowers/` scratch files.
- clang-format via pre-commit on staged files.

---

## Task 1: Wave 1 — Internal transitional alias removal + audit PR

**Goal:** Remove ADR-032 §1 Wave 1 deprecated forwards; grep gate clean.

**Audit (record in report):** For each symbol, `rg` `bcos-evm` + `bcos-evm/test` — must be zero call sites (header definition + ADR/docs only).

**Remove from headers (inline deprecated only):**

| Remove | File |
| --- | --- |
| `debitIntrinsicGas` | `eth/pipeline/DeductIntrinsicGas.h` |
| `runExecutionFrame` | `eth/execution/EvmCallFrame.h` |
| `TxExecutionRunner::run` | `eth/execution/TxExecutionRunner.h` |
| `buildExecuteMessageInput` | `eth/pipeline/EvmTxContextView.h` |
| `setupMessage`, `checkTransactionRules`, `checkGasAffordable`, `checkBalanceAndValue`, `tuneExecutionInput`, `runEvmExecution` | `eth/pipeline/ChainPrecheckPolicy.h` |
| `checkEntryRules` | `opstack/OpStackPrecheckPolicy.h` |
| `dispatchPrecompile` declaration + `.cpp` body if present | `eth/precompiled/PrecompileRouter.h` / `.cpp` |

**Also:**
- Trim `GethNamingAliases.h` Wave 1 retired symbol comments.
- Add ADR-032 appendix row: `2026-06-30 Wave 1` merged.
- Commit message: `refactor(evm): ADR-032 Wave 1 — remove internal transitional aliases`

**Tests:** Run smoke ctest above; build must pass.

---

## Task 2: Wave 2 — Kernel Tier E alias removal PR

**Goal:** Remove `runTxPipeline` and `executeMessage` deprecated forwards.

**Remove:**
- `runTxPipeline` from `eth/pipeline/StateTransitionExecute.h`
- `executeMessage` from `eth/execution/InnerExecute.h`

**Gate:** `rg '\brunTxPipeline\b|\bexecuteMessage\b' bcos-evm transaction-executor` — zero call sites (exclude `OpStackExecuteMessageTestHook` spy slot name if unrelated).

**Also:**
- Update `GethNamingAliases.h` Tier E comments for removed kernel symbols.
- ADR-032 appendix: Wave 2 row.

**Commit:** `refactor(evm): ADR-032 Wave 2 — remove kernel Tier E forwards`

---

## Task 3: Wave 3 — Chain entry flip PR

**Goal:** Export `apply*Message` as primary symbols; `*Execute` become `[[deprecated]]` inline forwards.

**For each adapter** (`FiscoExecute`, `EthReferenceExecute`, `OpStackExecute`):

1. Rename implementation function body to `apply*Message` (exported).
2. Add `[[deprecated]] inline *Execute(...)` forwarding to `apply*Message`.
3. Update `@brief`, log `LOG_DESC` strings to canonical names.
4. Keep `.cpp` filename unchanged for now (Wave 5 may rename).

**TE hygiene (optional, in same PR):** Update comments in `transaction-executor/` to cite `apply*Message`.

**Gate:** TE still calls `apply*Message` only (verify).

**Commit:** `refactor(evm): ADR-032 Wave 3 — promote apply*Message exports`

---

## Task 4: Wave 4 — Remove *Execute forwards PR

**Goal:** Delete deprecated `fiscoExecute`, `ethReferenceExecute`, `opStackExecute` inline forwards.

**Gate (monorepo):**
- `rg '\bfiscoExecute\b|\bethReferenceExecute\b|\bopStackExecute\b'` — zero hits except ADR/history.
- Migrate any remaining test call sites to `apply*Message`.
- Update aggregate headers: `include/bcos-evm/fisco_executor.hpp`, `eth_executor.hpp`, `op_executor.hpp`.

**Commit:** `refactor(evm): ADR-032 Wave 4 — remove *Execute Tier E symbols`

---

## Task 5: Wave 5 — Documentation cleanup PR

**Goal:** Doc/test/aggregate header final sweep.

- ADR-030 §8 stable-alias table: mark removed symbols with date.
- `architecture-overview.md`, chain READMEs, ADR-031 appendix timeline.
- `GethNamingAliases.h`: remove stale Tier E index entries.
- CI confirmation note in ADR-032 appendix.

**Commit:** `docs(evm): ADR-032 Wave 5 — retirement doc cleanup`

---

## Verification (all waves)

```bash
cd build && ctest -R 'EthOrchestrationProfile|TxPipeline|TxExecution|Precompile|OpStackPrecheck|DeductIntrinsic|ExecuteViaHost|GethNaming' --output-on-failure
```
